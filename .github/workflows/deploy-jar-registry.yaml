name: Deploy jar registry

on:  # yamllint disable-line rule:truthy
  workflow_call:
    inputs:
      NAME:  # name of service, (ex: peeq-tracking)
        required: true
        type: string
      VERSION:
        required: true
        type: string
      MVN_ARGS:
        default: 'clean deploy'
        required: false
        type: string
      JAVA_VERSION:
        default: '17'
        required: false
        type: string

    # secrets:
      # GH_TOKEN:  # GitHub token
      #   required: true

jobs:

  deploy-jar-registry:
    timeout-minutes: 15
    permissions:
      contents: 'read'
      id-token: 'write'
      packages: 'write'
    runs-on: ubuntu-latest
    steps:

      - name: "☁️ Checkout repository"
        uses: actions/checkout@v3

      - name: "🔧 Set up JDK ${{ inputs.JAVA_VERSION }}"
        uses: actions/setup-java@v3
        with:
          java-version: '${{ inputs.JAVA_VERSION }}'
          distribution: 'temurin'

      - name: "🔨 Maven set version"
        run: |
          mvn versions:set -DnewVersion=${{ inputs.VERSION }}

      - name: "🔨 Build with Maven"
        run: mvn ${{ inputs.MVN_ARGS }}

#       - name: "🌐 Link to Lib"
#         env:
#           GH_TOKEN: ${{ secrets.GH_TOKEN }}
# yamllint disable rule:line-length
#           # https://console.cloud.google.com/artifacts/maven/ShipMagic/us-central1/peeq-java/com.peeq:peeq-shared-lib/0.0.0-SNAPSHOT-PR-147-2?project=ShipMagic
#           GAR_URL: ${{ inputs.GAR_MAVEN_URL }}${{ inputs.PROJECT_ID }}/${{ inputs.GAR_LOCATION }}/${{ inputs.MAVEN_REGISTRY }}/${{ inputs.JAR_PATH }}:${{ inputs.NAME }}/${{ inputs.VERSION }}?project=${{ inputs.PROJECT_ID }}
# yamllint enable rule:line-length
#         run: |-
#           echo "${GAR_URL}"
#           if [ "${{ github.event_name }}" = "pull_request" ]; then
#             gh pr comment ${{ github.event.number }} \
#             --body "📦 JAR Lib: [${{ inputs.NAME }} ${{ inputs.VERSION }}](${GAR_URL})"
#           fi
